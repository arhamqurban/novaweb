import { FAQManagerClient } from "@/components/admin/faq-manager-client";

export const metadata = {
  title: "FAQ Manager",
};

export default function AdminFAQPage() {
  return <FAQManagerClient />;
}
