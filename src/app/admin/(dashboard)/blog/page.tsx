import { BlogManagerClient } from "@/components/admin/blog-manager-client";

export const metadata = {
  title: "Blog Manager",
};

export default function AdminBlogPage() {
  return <BlogManagerClient />;
}
